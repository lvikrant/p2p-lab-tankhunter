package p2p.sockets;

public class NetworkTarget {

		String host;
		int port;
		
		public NetworkTarget(String _host, int _port)
		{
			host = _host; //IP
			port = _port; //open Port
		}
}
